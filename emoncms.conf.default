# name of Node in EmonCMS Inputs
node = "xxxxxxxxx";

# IP address of GSM/UMTS modem
modemIP = "192.168.8.1";

# host to send data to
host = "xxxxxxxxxxxx";

# write API key for writing into database, from http://[host]/user/view
apikey = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx";
